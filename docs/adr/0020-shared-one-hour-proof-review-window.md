# Use one Proof Review Window for every Quest

Every Quest mode gives the Hirer one hour to review a Proof Submission or proof-free completion confirmation. After that window, the lifecycle worker auto-approves the pending work and uses the normal atomic completion and payment flow; one shared deadline keeps payment timing predictable across Quest modes.
