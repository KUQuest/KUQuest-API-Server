# Keep Quest API v1 out of Quest API v2

Quest API v1 is deprecated and has no remaining consumer or database
dependency for this contract. Quest API v2 therefore owns its canonical
contracts without mapping v1 fields, rows, or semantics and without backward
compatibility; existing v1 code remains unchanged until separate removal work.

Status: accepted.
